import { PageSkeleton } from '@/components/ui/Skeleton'

/** Finance content-area fallback — see components/ui/Skeleton.tsx for why these exist. */
export default function Loading() {
  return <PageSkeleton />
}
