import { PageSkeleton } from '@/components/ui/Skeleton'

/** Store content-area fallback — see components/ui/Skeleton.tsx for why these exist. */
export default function Loading() {
  return <PageSkeleton />
}
